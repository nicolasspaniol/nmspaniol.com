[project]
name = "nmspaniol-com"
version = "0.1.0"
authors = [
    { name = "nmspaniol", email = "nicolasmspaniol@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.1.0",
]

[project.scripts]
nmspaniol-com = "nmspaniol_com:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
